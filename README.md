# Chi_Square_Mini_Project
Chi-Square Test based project to analyze whether vehicle type affects booking success in a rental system. The project reads real data from a CSV file, performs both manual and SciPy-based calculations, and determines if variables are statistically dependent or independent.
